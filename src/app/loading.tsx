// Minimal global loading — just a top-level pulse bar.
// Each page handles its own skeleton via Suspense internally.
// This file intentionally has no content so it doesn't interfere
// with per-page skeletons or the sidebar layout.
export default function Loading() {
  return null
}
